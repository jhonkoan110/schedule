import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
    administration: {
        marginTop: '1rem',
    },
    content: {
        padding: '1rem',
        backgroundColor: '#fff',
    },
    card: {
        width: '100%',
        padding: '2rem',
        display: 'flex'
    }
}));

export default useStyles;
